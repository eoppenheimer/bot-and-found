{
    "scripts": {
        "build": "esbuild src/server.ts --outfile=dist/server.js --target=es2022 --platform=node --bundle --minify;",
        "start": "npm run build && node ./dist/server.js",
        "docker": "docker build -t eoppenheimer/bot-and-found:latest . && docker compose up"
    },
    "dependencies": {
        "@desmodder/graph-state": "^0.2.3",
        "better-sqlite3": "^12.4.1",
        "dotenv": "^17.2.2",
        "esbuild": "^0.25.10",
        "express": "^5.1.0",
        "express-session": "^1.18.2",
        "mongodb": "^6.20.0",
        "prompt-sync": "^4.2.0",
        "typescript-eslint": "^8.44.1"
    },
    "devDependencies": {
        "@types/better-sqlite3": "^7.6.13",
        "@types/express": "^5.0.3",
        "@types/express-session": "^1.18.2",
        "@types/mongodb": "^4.0.6",
        "@types/prompt-sync": "^4.2.3"
    }
}
