{
    "scripts": {
        "build": "esbuild src/server.ts --outfile=dist/server.js --loader:.txt=text --loader:.sql=text --target=es2022 --platform=node --bundle --packages=external",
        "start": "npm run build && node ./dist/server.js",
        "docker": "tsc --noEmit && docker build -t eoppenheimer/bot-and-found:latest . && docker compose up"
    },
    "dependencies": {
        "@desmodder/graph-state": "^0.2.3",
        "@types/better-sqlite3": "^7.6.13",
        "better-sqlite3": "^12.4.1",
        "csv-parser": "^3.2.0",
        "dotenv": "^17.2.2",
        "esbuild": "^0.25.10",
        "express": "^5.1.0",
        "express-session": "^1.18.2",
        "mongodb": "^6.20.0",
        "prompt-sync": "^4.2.0",
        "typescript-eslint": "^8.44.1"
    },
    "devDependencies": {
        "@types/express": "^5.0.3",
        "@types/express-session": "^1.18.2",
        "@types/mongodb": "^4.0.6",
        "@types/prompt-sync": "^4.2.3"
    }
}
